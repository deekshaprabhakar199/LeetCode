        }
            }
                if(curr != '.'){
                char curr = board[i][j];
                    if(!position.add("Row"+i+curr) || !position.add("Col"+j+curr) || !position.add("Grid"+i/3+j/3+curr)){
                }
                        return false;
                    }
        return true;
    }
}
            for(int j=0; j< board[0].length;j++){
        for(int i=0; i< board.length;i++){
[
