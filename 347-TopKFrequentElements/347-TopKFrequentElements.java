                    }
                }
            }
        }
        
                        break;
                    if (index == k) {
                    result[index++] = num;
                for (int num : buckets[i]) {
            if (buckets[i] != null) {
        for (int i = buckets.length - 1; i >= 0 && index < k; i--) {
        int index = 0;
        int[] result = new int[k];
[
