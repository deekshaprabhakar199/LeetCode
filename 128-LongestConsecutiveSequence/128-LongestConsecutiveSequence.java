            if(!unique.contains(i-1)){
                currLength = 1;
                while(unique.contains(i+1)){
                    currLength++;
                }
                    i++;
                maxLength = Math.max(currLength, maxLength);
        for(int i: nums){
        }
            unique.add(i);
        for(int i: nums){
            }
        }
        return maxLength;
    }
}
[
