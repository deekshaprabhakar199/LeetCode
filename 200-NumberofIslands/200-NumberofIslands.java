        }
            }
                }
                    dfs(grid, i, j);
                    islandCount++;
                if(grid[i][j]=='1'){
            for( int j=0; j< cols; j++){
        for(int i=0; i<row; i++){

        int islandCount = 0;
        int cols = grid[0].length;
        return islandCount;
    }
        int row= grid.length;
    public int numIslands(char[][] grid) {
[
